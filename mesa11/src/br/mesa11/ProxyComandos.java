package br.mesa11;


public class ProxyComandos {

	public ProxyComandos() {
	}

	public Object processarObjeto(Object object) {
		// AlgolTO algolTO = (AlgolTO) object;
		// if (ConstantesAlgol.LOGAR.equals(algolTO.getComando())) {
		// return controleLogin.logar((Usuario) algolTO.getData());
		// } else if (ConstantesAlgol.NOVO_USUARIO.equals(algolTO.getComando()))
		// {
		// return controleLogin.cadastratUsuario((Usuario) algolTO.getData());
		// } else if (ConstantesAlgol.SALVAR_MAPA.equals(algolTO.getComando()))
		// {
		// return controleMapa.salvarMapa((DadosMapa) algolTO.getData());
		// } else if (ConstantesAlgol.CARREGAR_LISTA_MAPAS.equals(algolTO
		// .getComando())) {
		// return controleMapa.carregarListaMapas((Long) algolTO.getData());
		// } else if
		// (ConstantesAlgol.CARREGAR_MAPA.equals(algolTO.getComando())) {
		// return controleMapa.carregarMapa((String) algolTO.getData());
		// } else if (ConstantesAlgol.APAGAR_MAPA.equals(algolTO.getComando()))
		// {
		// return controleMapa.apagarMapa((Long) algolTO.getData());
		// } else if (ConstantesAlgol.SALVAR_NPC.equals(algolTO.getComando())) {
		// return controleNpc.salvarNpc(algolTO);
		// } else if (ConstantesAlgol.APAGAR_NPC.equals(algolTO.getComando())) {
		// return controleNpc.apagarNpc((Long) algolTO.getData());
		// } else if (ConstantesAlgol.CARREGAR_LISTA_NPCS.equals(algolTO
		// .getComando())) {
		// return controleNpc.carregarListaNpcs(algolTO);
		// } else if
		// (ConstantesAlgol.PESQUISAR_NPC.equals(algolTO.getComando())) {
		// return controleNpc.pesquisarNpc(algolTO);
		// } else if (ConstantesAlgol.CARREGAR_LISTA_MAPA_NPC.equals(algolTO
		// .getComando())) {
		// return controleMapa.carregaListaMapaNpc((Long) algolTO.getData());
		// } else if (ConstantesAlgol.CARREGAR_LISTA_PORTAIS.equals(algolTO
		// .getComando())) {
		// return controleMapa.carregaListaPortais((Long) algolTO.getData());
		// } else if (ConstantesAlgol.ADICIONAR_NPC_MAPA.equals(algolTO
		// .getComando())) {
		// return controleMapa.salvarMapaNpc((Encontro) algolTO.getData());
		// } else if (ConstantesAlgol.ADICIONAR_PORTAL_MAPA.equals(algolTO
		// .getComando())) {
		// return controleMapa.salvarPortal((Portal) algolTO.getData());
		// } else if (ConstantesAlgol.SALVAR_DETALHES_MAPA.equals(algolTO
		// .getComando())) {
		// return controleMapa.salvarDetalhesMapa((Mapa) algolTO.getData());
		// } else if
		// (ConstantesAlgol.APAGAR_NPC_MAPA.equals(algolTO.getComando())) {
		// return controleNpc.apagarNpcMapa((Long) algolTO.getData());
		// } else if (ConstantesAlgol.APAGAR_PORTAL_MAPA.equals(algolTO
		// .getComando())) {
		// return controleMapa.apagarPortalMapa((Long) algolTO.getData());
		// } else if (ConstantesAlgol.SALVAR_ITEM.equals(algolTO.getComando()))
		// {
		// return controleItem.salvarItem(algolTO);
		// } else if
		// (ConstantesAlgol.PESQUISAR_ITEM.equals(algolTO.getComando())) {
		// return controleItem.pesquisarItem(algolTO);
		// } else if (ConstantesAlgol.CARREGAR_LISTA_ITENS.equals(algolTO
		// .getComando())) {
		// return controleItem.carregarListaItems(algolTO);
		// } else if (ConstantesAlgol.APAGAR_ITEM.equals(algolTO.getComando()))
		// {
		// return controleItem.apagarItem((Long) algolTO.getData());
		// }
		return null;
	}
}
